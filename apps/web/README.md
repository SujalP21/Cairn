# @cairn/web

The Cairn web client — React 18 + Vite, styled with [Primer](https://primer.style/).

Run it from the repository root so npm workspaces resolves dependencies correctly:

```bash
npm run dev:web
```

See the [root README](../../README.md) for architecture, environment variables, and full setup.
